version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "${FASTAPI_PORT:-8010}:8010"
    env_file: .env
    volumes:
      - ./backend:/app/backend
      - uploads:/app/uploads
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8010 --reload

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "${STREAMLIT_PORT:-8511}:8511"
    env_file: .env
    environment:
      - BACKEND_URL=http://backend:8010
    volumes:
      - ./frontend:/app/frontend
    command: streamlit run frontend/app.py --server.port 8511 --server.address 0.0.0.0
    depends_on:
      - backend

volumes:
  uploads:
